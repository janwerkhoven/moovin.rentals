name: Continuous Integration

on:
  push:
    branches:
      - "**"
      - "!production"
  pull_request:
    branches:
      - "**"
      - "!production"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'echo "TODO"'
